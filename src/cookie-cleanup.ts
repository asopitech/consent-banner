const patterns=[/^_ga$/, /^_ga_/, /^_gid$/, /^_gat$/, /^_gac_/, /^_gcl_/];
export function cleanupGaCookies():void{try{const names=document.cookie.split(';').map(c=>c.split('=')[0]?.trim()).filter(Boolean).filter(n=>patterns.some(p=>p.test(n))); const paths=Array.from(new Set(['/', location.pathname])); const domains=['', location.hostname, '.'+location.hostname]; for(const name of names)for(const path of paths)for(const domain of domains){document.cookie=`${name}=; Max-Age=0; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=${path}${domain?`; domain=${domain}`:''}; SameSite=Lax`}}catch{}}

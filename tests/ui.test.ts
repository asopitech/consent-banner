import '../src/index';
async function boot(attrs='data-measurement-id="G-ABC123" data-policy-url="/privacy/"'){document.body.innerHTML=`<script ${attrs}></script>`; Object.defineProperty(document,'currentScript',{value:document.querySelector('script'),configurable:true}); delete window.__ASOPI_CONSENT_READY__; await import('../src/index.ts?'+Math.random())}
test('unknown shows banner and reject stores denied',async()=>{await boot(); expect(document.querySelector('.asopi-consent-banner')).toBeTruthy(); (document.querySelectorAll('button')[1] as HTMLButtonElement).click(); expect(window.AsopiConsent?.getState()).toBe('denied'); expect(document.querySelector('.asopi-consent-banner')).toBeFalsy()});
test('settings dialog opens and escape closes',async()=>{await boot('data-measurement-id="G-ABC123" data-language="en"'); window.AsopiConsent?.openSettings(); expect(document.querySelector('dialog')?.open).toBe(true); document.querySelector('dialog')?.dispatchEvent(new KeyboardEvent('keydown',{key:'Escape'})); window.AsopiConsent?.closeSettings(); expect(document.querySelector('dialog')?.open).toBe(false)});
test('reset shows banner again',async()=>{await boot(); window.AsopiConsent?.deny(); window.AsopiConsent?.reset(); expect(window.AsopiConsent?.getState()).toBe('unknown'); expect(document.querySelector('.asopi-consent-banner')).toBeTruthy()});
